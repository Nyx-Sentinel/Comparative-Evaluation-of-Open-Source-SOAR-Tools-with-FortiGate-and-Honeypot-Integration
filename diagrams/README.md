# Diagrams

- `architecture.svg` — Full testbed architecture diagram (SVG format)
- `architecture.png` — Export the SVG as PNG and place here for README image embedding

To convert SVG to PNG:
```bash
inkscape architecture.svg --export-png=architecture.png --export-dpi=150
```
Or use any online SVG to PNG converter.
